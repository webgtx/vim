ab sockserv import socket<CR><Home><CR><Home>HOST = "192.168.1.98"<CR><Home>PORT = 9090<CR><Home><CR><Home>server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)<CR><Home>server.bind((HOST, PORT))<CR><Home><CR><Home>server.listen(5)<CR><Home>print(f"Server is running on {HOST}:{PORT}")<CR><Home><CR><Home>while True:<CR><Home>    communcation_sock, addr = server.accept()<CR><Home>    print(f"Connected to {addr}")<CR><Home>    msg = communcation_sock.recv(1024).decode("ascii")<CR><Home>    print(f"Client says: {msg}")<CR><Home>    communcation_sock.send("Got you, thanks".encode('ascii'))<CR><Home>    communcation_sock.close()<CR><Home>    print(f"Connection with {addr} was closed") <CR><Home>
ab ezyaml import yaml<CR><Home>with open(argv[1], "r") as stream:<CR><Home>  data = yaml.safe_load(stream)<CR><Home>
