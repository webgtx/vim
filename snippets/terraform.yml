- name: azprov
  code: |
    terraform {
      required_providers {
        azurerm = {
          source = "hashicorp/azurerm"
          version = "3.77.0"
        }
      }
    }
    
    provider "azurerm" {
      # Configuration options

- name: awsprov
  code: |
    terraform {
      required_providers {
        aws = {
          source = "hashicorp/aws"
          version = "5.22.0"
        }
      }
    }
    
    provider "aws" {
      # Configuration options
    }
